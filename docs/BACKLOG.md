# Backlog

One line per raw idea. Picking one up means `/idea` (needs thinking) or
`/plan` (obvious); the line is removed then — as is a line a roadmap cycle
has committed to, which now lives in `docs/ROADMAP.md` instead. Rejected
ideas keep one line below with the reason, so the same idea is not
re-brainstormed.

- A typed lineage value (`Provenance::lineage(output)`, a tree down to `Role`s), if a second consumer asks for one rather than walking the record itself (ADR-0009 alternative)
- `cargo-semver-checks` in CI once the first non-placeholder version is published
- A `no_std`-friendly `arris-math`, if an embedded or wasm consumer ever wants it
- NURBS degree elevation as a primitive edit (data-model §NURBS names it; no C1 step needs it — knot insertion is enough for M1's fitting)
- Knot insertion on a periodic NURBS that keeps the wrap: today the result's knots no longer imply a period and it extrapolates outside its domain (data-model §NURBS); needed once a periodic curve from STEP is edited
- S5's coincident-surface arm tests face overlap on a grid of interior points carried through 3D, so an overlap thinner than the grid spacing passes; an exact (u, v) region intersection replaces it once a boolean can produce such faces (M4 finding candidate). Reviewed at M4 step 10: the boolean's exact overlap test is the (u, v) arrangement in `arris-ops`, above `arris-check`, so S5 cannot call it; the exact test for the checker is a polygon–polygon overlap in `region2` (segment crossings plus one containment), still to write
- `Builder::finish` makes `Solid` only; `Sheet` needs an operator that leaves an edge with one use (a `mev` strut not closed by a `mef`), which no C1 operation asks for — with the healing cycle's sheet bodies
- STEP cannot carry a left-handed pcurve conic (`AXIS2_PLACEMENT_2D` is direct) or a degenerate edge's coedge; both are dropped on write (architecture §Formats and tools). The reader cycle must rebuild them from the 3D curve and the surface's singularity
- `TriMesh::push_position` casts the position count to `u32` with no bound, so a mesh past four billion vertices wraps silently instead of returning a typed error (M3 finding, confirmed by the kernel-seams review)
- `obj::write` offsets each mesh's `f` indices by a running `u32` total of the positions and corners before it, so several meshes past four billion vertices together overflow it (a panic in debug, a wrap in release) though each fits alone; a typed `MeshWriteError` as binary STL's count has (C2 close-cycle drift review)
- A NURBS surface has no `project` (cycle 1, by design), so a NURBS face's mesh deviation can only be measured against a closed form; the tessellation property test covers the analytic kinds and one hand-built saddle (M3 finding). The reader cycle needs it before that: a pcurve is rebuilt by projection for every edge of every face read, NURBS faces included (ADR-0020)
- The boolean's remaining hot passes are sequential: the edge-on-face hits of the pave model and the ray cast that classifies every piece (`classify_point`), plus the checker that runs after every operation in debug builds. `parallel` covers the face-pair intersections and the per-face splitting and buys 1.4× on the eight-cut bolt pattern; the hits are a candidate for the same treatment, and the classifier needs its ray casts to stay in a fixed order (M4 step 13 finding)
- Adaptive or curvature-driven mesh refinement beyond a chord tolerance, if a consumer's mesh sizes come out too coarse or too dense against `arris-mesh`'s uniform (u, v) grid (M3 non-goal)
- Oblique extrusion, refused today as `Reason::DirectionNotNormal`: a line sweeps a plane at any angle, but an arc sweeps a cylinder of elliptical section, which `Surface::EllipticCylinder` (ADR-0014) holds and no sweep builds yet — with the sweep cycle's sweep along a path (M5 `⚠ OPEN` 3)
- Chamfers with two distances, or a distance and an angle (`BRepFilletAPI_MakeChamfer`'s other modes): `ops::chamfer` takes one distance measured on both faces (fillet-and-chamfer non-goal)
- `Reason` grouped by operation (blend, boolean, sweep, primitive) rather than one flat enum, a breaking change best made with the next plan that adds variants (fillet-and-chamfer design delta)
- A revolve of an elliptic profile segment, refused today as `Reason::EllipticRevolve` (ADR-0014): it needs a surface of revolution with an elliptic meridian — a variant or a `Nurbs` — and ellipse sections in the meridian arm's table (ADR-0008), with its own corpus; for the first consumer or corpus that revolves an ellipse (facade-swap step 3)
- Blends on face pairs outside ADR-0007's table (a plane or cylinder against a cone, sphere, torus or NURBS face; two curved faces): the first consumer's old backend blends some of these, so its side-by-side run will find them as regressions (ADR-0017) — the blend-network cycle, or earlier as fixtures
- A spindle torus (`R < r`) as a surface, so a revolve of an arc whose circle crosses its axis is more than `Reason::SpindleTorus`, when an operation needs one (M5 non-goal)
- `ops::planar_face`, a `Profile` as a one-face sheet body, with the healing cycle's sheet bodies; the sweeps' cap builder is private until then (M5 `⚠ OPEN` 1)
- Lumps stored on the `Body` entity and in the native format rather than derived by `arris_check::lumps`: every STEP write and corpus count of a multi-shell body re-runs B1's face-pair meeting test and ray casts, so storing becomes worth it once that shows up as a cost (multi-shell `⚠ OPEN` 3)
- Split `arris-debug` into a light testkit (samples, dump, prop harness, geometry and profile strategies, expressions) above `arris-topo`, a corpus crate, and the renderer: today it pulls ops, mesh, io and `image` into every lower crate's test build (needs an idea and an ADR; kernel-seams review)
- `GeomError::Degenerate { reason: String }` → a typed `DegenerateReason` enum, with `basis::validate`, `Spline::new` and `FitError` returning it; tests match variants, not substrings (kernel-seams review)
- `BuildError`'s 39 variants nested by API (operators, `finish`, `assemble`); `primitive::line` given its own error instead of `BuildError::Empty` (kernel-seams review)
- The boolean's `side: usize` and `Option<bool>` selections → `enum Operand { A, B }` and `enum Selection { Drop, Keep, KeepReversed }`, including the public `EdgeImage::side`; with the next plan that touches `Op::select` (kernel-seams review)
- ops and io reach math, geom and topo through `arris_check::arris_topo::…` re-export chains (122 paths in ops): declare the direct, layer-legal dependencies and hide the chain re-exports (kernel-seams review)
- Narrow `pub` internals: ops `boolean` interference types, topo `Staged*`/`RawInsert`/`CHUNK_SIZE`, geom tuning constants and `segments_intersect`/`interior_knots`, check `Report::with_*`; one export scheme per crate (kernel-seams review)
- Split the files that hold several concerns, each when a feature next touches it: `topo/builder.rs` (refs, staged, error, euler, finish, assemble, validate), `ops/sweep.rs` (one skeleton for extrude and revolve), `boolean/pave.rs` (hits, sections, coincident, each phase a typed output), `debug/corpus.rs` (one function per stage), `check/check.rs` (rows by entity), `mesh/cdt.rs` (locate/insert/legalize, constraint recovery, classification), `geom/pcurve.rs` (plane, revolution, fitted) (kernel-seams review)
- A miter of two blends with unequal dihedrals (a slanted prism's vertical edge and its cap edge): the two far contacts meet the sharp third edge at two points, so the corner is the cylinders' ellipse plus a second arc where the wider blend is trimmed by the face across, and the third face takes that arc — refused as `VertexBlend` since fillet-and-chamfer step 2; the blend-network cycle's vertex blends
- A miter where a plane–cylinder ruling blend meets another blend (a half disc's chord–arc edge and its chord's cap edge): the ruling blend's contact on the cylinder meets the sharp third edge — a circle — away from the other blend's contact, so the corner is two arcs — refused as `VertexBlend` since fillet-and-chamfer step 7; the blend-network cycle's vertex blends
- A chamfer of a plane–cylinder edge along a ruling (`ChFiKPart`'s plane–cylinder chamfer): a plane through the contact line on the plane and a ruling of the cylinder, the distance on the cylinder decided as an arc or a chord — `OpError::Unsupported` since fillet-and-chamfer step 7, outside the table
- A fillet corner of three planes none of which is square to the other two (the corner where a cube's vertical edge meets an oblique cut, all three filleted): the sphere about the ball's centre is the same, but no frame puts every side on its equator or a meridian, so one side is a tilted great circle with a fitted pcurve on the sphere — refused as `VertexBlend` since fillet-and-chamfer step 9, while three chamfers there build. Its blocker is gone: `pcurve_on` fits a pcurve on a sphere over the projection since c3-conic-hits step 1, so what is left is the corner's own construction; the blend-network cycle's vertex blends
- The discretisation chord from the surface's speed over the face's (u, v) box (`Surface::speed_bounds`), not at the first pcurve's first point, before NURBS faces or blends on tori with a large R/r ratio (kernel-seams review)
- ADR-0005's ribbon flattening detects a ruled direction only through an infinite `chord_steps`, so a ruled NURBS never gets it: a `Surface::is_ruled(direction)` query (kernel-seams review)
- Check tests by topic, not by the plan step that wrote them: `fast_part1`/`fast_part2` into row-family files over a shared `tests/common`; `ops/tests/boolean.rs` (two files joined) and `revolve.rs` split; a `corpus_tests!` macro checked against `fixtures::corpus()` instead of two hand lists (kernel-seams review)
- The workspace paths derived five ways into one `paths` module honouring `CARGO_TARGET_DIR` (kernel-seams review)
- S5 and B1 compare every face pair behind a box test: sort-and-sweep the boxes before the intersector (kernel-seams review)
- Two intersector properties fail at 2048 cases, both test-side: `random_plane_and_cylinder_agree_on_every_common_property` holds an ellipse 4.5e5 from the origin to an absolute 1e-10, and `coaxial_pairs_meet_where_their_meridians_meet` brackets a steep cone's apex kink between two meridian samples; scale the first bound with the point's magnitude and sample the apex in the second (quadric-checker-arms step 3 finding)
- A multi-shell boolean assembles its result twice: `lump_order` finishes a scratch copy of the model to read `arris_check::lumps`, then the result is assembled again in lump order; `lumps` over the assembly's shells before `finish` would build it once (kernel-seams review)
- `Builder` kill operators and `Slots::len` are linear in the whole builder: a live slot count and an incremental edge-use index, once a blend runs local operators on large bodies (kernel-seams review)
- Mass properties that do not depend on where the body is: the boundary integral runs over each face's own fitted pcurves, which meet their neighbours' only within the edge's tolerance, so the gaps between them are measured at their distance from the origin — two crossing cylinders' common 108 from it has its centroid 4.6e-6 off the exact centre when the seam runs through a crossing vertex and 1.5e-7 at a generic turn, both 1.4e-10 at the origin, and the first is past `fitted_rel`'s bound. Measuring about a point of the body (its box centre) and moving the moments back is the likely fix; `mass_properties`' own accuracy claim and `fitted_rel`'s doc then change with it (plans/seam-parametrisation-faults step 6 finding)
- The property case count the pre-commit hook runs (256) against CI's (1000): the seam-parametrisation fault sat in `main` until the first CI run because its counterexample lives past case 32 of a shard, and cost a half-published 0.1.0. Raise the hook's count, keep it and accept CI as the first catch, or add a nightly run above CI's — the human's call, put by the measuring harness plan, which holds the nightly tier (plans/seam-parametrisation-faults open question)
- Two property tests of the closed-form surface table fail at `ARRIS_PROPTEST_CASES=3000` (default seed), pass at CI's 1000, and fail the same on the commit before the quadric-curves plan: `random_plane_and_cylinder_agree_on_every_common_property` (a grazing plane's ellipse centred 4.5e5 away, 1.1e-10 off against the test's absolute 1e-10 — likely the bound, not the ellipse) and `coaxial_pairs_meet_where_their_meridians_meet` (a near-flat cone, α = 80°, against a thin coaxial cylinder: a crossing the sampled meridians see has no circle) — to shrink into regression fixtures (quadric-intersection-curves step 5 finding)
- Two cones sharing an apex meet in up to four rulings through it, and the tracer refuses the pose as `SectionFault::SharedRuling`; a closed form (the rulings where the two cones' directions agree) when a consumer's cones meet so (quadric-intersection-curves step 5 finding)
- A section beside an apex or a pole is refused, not built (`Reason::BesideSingularity`, ADR-0021; `regression/ball-beside-pole-slice-cut`): its pcurve turns `u` by up to π over a stretch as long as the miss, which `pcurve_on` fits and the (u, v) polygons do not resolve — `region2` cuts a piece evenly in its parameter by the *largest* second derivative anywhere on it, caps at 65536 segments, and reports a deviation bound larger than the face, so the split finds no interior point after tens of seconds (built from a miss of 1e-4 on a ball of radius 2, not at 1e-5). What lifts it: a segment count per knot span from that span's own curvature — the fit already halves its spans where `u` swings — and a chord converted to length where the pcurve is, not once per face at its first pcurve's first point, which on a sphere is a pole; both reach the checker's polygons, tessellation and `measure`, so it is a plan of its own with the measuring harness beside it (c3-conic-hits step 6 finding)
- `Curve::Nurbs` against `Curve::Nurbs` in `intersect_curves`, an explicit `Unsupported` arm today (two fitted sections are met through the tracer's points instead): the NURBS cycle's marcher needs it first (quadric-intersection-curves non-goal)
- The procedural `Curve::Intersection { surfaces, chart }` on the fitted section's spline as its chart, ADR-0018's upgrade path (option C): taken when a tripwire fires — a fixture whose tolerances grow through chained booleans across a fitted edge, or a consumer re-evaluating at a finer precision than the model was built at (ADR-0018)
- A section fit that reuses the tracer's samples across the face pairs on the same two surfaces: one region per boolean already makes their curves bit for bit the same, but each pair traces and fits again — if profiling asks (quadric-intersection-curves docs list)
- A closed NURBS curve that is not periodic, met by a surface at its seam, is reported at both ends as two hits of one point (data-model §Curves); no curve the kernel makes is one, a STEP reader's would be — with the reader cycle (quadric-intersection-curves step 6 finding)
- A loop of a torus section within about 1e-4 of a tube circle all the way round is `SectionFault::UnresolvedTurning`: the march follows graphs `v(u)` and that loop is a graph `u(v)`, so it needs a march along `v` as well, chosen per stretch. Measured on the 2 by 0.5 ring from one tolerance to 1e-4 off each of step 3's seven holder poses; clean from 1e-3 (c3-torus-pairs step 3 finding)
- Two tube circles on the other surface with more of the section besides (an elliptic cylinder along a chord of the centre circle) is `SectionFault::TubeCircle`: the quotient by both factors has no closed form to walk, so the residual branches cannot be traced. Two circles with nothing else is returned (c3-torus-pairs step 3 finding)
- A cone off a tube-circle pose by less than the tolerance with its apex by the torus is `SectionFault::TubeCircle`: `Implicit::firmness`, the lower bound of `|∇F|` on the surface that holds the residual within `tol.linear` once the circle is divided out, has no bound by a cone's apex (c3-torus-pairs step 3 finding)
- The Villarceau circles of a bitangent plane come back as four fitted arms through two singular points, not as exact `Curve::Circle`s: a "bitangent within `tol.linear`" detector would have to stay consistent with the tracer's own singular-point rule, for one pose of measure zero (ADR-0019, c3-torus-pairs open question 2)
- `bernstein2` — tensor Bernstein polynomials on `[0, 1]²` with subdivision, exclusion and Krawczyk-certified isolation of two polynomials' common zeros, crate-private in `arris-geom` — is what C4's marcher puts a NURBS patch into an implicit with; the substitution in `implicit` is already generic over one variable or two (c3-torus-pairs step 1)
- The oracle refuses a curved pcurve on a surface of linear extrusion by name: `measure.py`'s area of such a face is the Green contour integral of the basis arc length against `dv`, exact where the trim is rulings and arcs, and a boolean that cuts an elliptic cylinder with anything else — two crossing elliptic prisms, whose walls meet in two conics — has no oracle. It wants a quadrature of that integral along the pcurve, and Open CASCADE's own measurement of the same shape is 6e-4 (fixed order) to 1.2e-3 (adaptive) out, so the fixture cannot be checked against it either (c3-conic-hits step 4 finding)
- Two surfaces, a curve and a surface, or a point and a surface a hair off parallel, coincident or tangent have no verdict: two planes at an angle under the angular tolerance but apart, a circle or a line a hair off a plane, a vertex a hair off one, a plane a hair off tangent to a cylinder are `OpError::Unsupported`, and a torus section at a near-tangency is `Fault::Geometry` ("degenerate: tangent along a curve"), a cylinder is placed with a non-finite frame, an ellipse lands 1.2e-7 off its surfaces — where the band wants the flush or the generic body. 1236 of the band survey's 4312 failures, the faces level of c3-tolerance-apart's goal that none of its steps takes (`regression/flush-union-a-tolerance-off`, `boss-flush-tilted`, `boss-flush-posed-gap-fuse`, `pipe-elbow-a-tolerance-off`, `coaxial-fuse-a-tolerance-off`, `coaxial-fuse-tilted-frame`, `tangent-hole-a-tolerance-out`, `tangent-outside-tilted-posed-fuse`). An edge-on-edge overlap one or two tolerances deep (plane against point) has no fixture: Open CASCADE builds a shell of odd Euler characteristic there too (plans/c3-tolerance-apart step 1 finding)
- Material a tolerance or two thick between two walls a hair off tangent — the lens a pin makes poking through its bore, the sliver a tool makes grazing a face — is a piece the (u, v) polygons cannot resolve (`Fault::Split` NoInterior, Hole) or a loop of zero signed area (L4, L2): 560 of the band survey's failures. Whether such a sliver is built, left out of the flush body or refused by name is a decision per level, with the polygons by span beside it (`regression/pin-in-bore-a-tolerance-off`, `tangent-outside-a-tolerance-in`, `tangent-outside-grown-common`, `tangent-cylinders-overlap-common`) (plans/c3-tolerance-apart step 1 finding). The same between features a few tolerances apart that are no longer one: a seam just past the two tolerances within which its touch joins a crossing vertex has its two crossings as vertices of their own, and a common keeps the triangle between them and the vertex at some turns, L4 — two tolerances to 2.6 at a right angle, and the band of the seam-turn property kept out of it (`regression/seam-two-tolerances-from-crossing-common`, step 2b finding)
- The builder and the checker refuse what the band's near-tangent contacts leave them: an edge with one coedge (`Fault::Builder`, EdgeUses, 434 — 64 of them `tangent-hole`'s fuse and common at the exact contact, where the top face's section circle is tangent to its own edge), a shell that does not close, B1's missing outer shell, V2 and V3, shells that meet (`Fault::Lumps`, 27), a fit whose normal equations are singular: 488 of the band survey's failures (`regression/tangent-hole-fuse`, `coaxial-fuse-a-tolerance-off`, `tangent-cylinders-tilted-fuse`, `tangent-outside-tilted-posed-common`, `edge-touching-tilted-posed-cut`) (plans/c3-tolerance-apart step 1 finding)

## Rejected
